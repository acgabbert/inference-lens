.env
.env.*
.git
.DS_Store
.next
.vinext
.wrangler
build
dist
out
node_modules
src-tauri/target
*.tsbuildinfo
*.log
npm-debug.log*
coverage

# Gitignored locally, so CI never sees it, but a maintainer's `notes` symlink
# would otherwise enter the context and bake its target path into a layer.
notes
